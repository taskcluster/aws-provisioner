---
  id: http://schemas.taskcluster.net/aws-provisioner/v1/worker-type-deleted-message.json#
  $schema: http://json-schema.org/draft-04/schema#
  title: Worker Type Message
  description: Message reporting that a worker type was deleted
  type: object
  properties:
    workerType:
      description: |
        Name of the worker type which was deleted
      type: string
  required:
    - workerType
